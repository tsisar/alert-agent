apiVersion: v1
kind: ConfigMap
metadata:
  name: alert-agent
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "alert-agent.labels" . | nindent 4 }}
data:
  mcp.json: |
    {
      "mcpServers": {{ .Values.mcp.servers | toJson }}
    }